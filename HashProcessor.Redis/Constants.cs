namespace HashProcessor.Redis;

// These keys can be configured independently, but for the sake of simplicity they are part of the same shared project
public class Constants
{
    public const string HashSummaryKey = "hashes:summaries";
}
